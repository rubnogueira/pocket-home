import { Separator } from "../ui/index.ts";
import type { RowRenderProps } from "../types/pocket-component.ts";

export default function DividerRow(_props: RowRenderProps) {
  return <Separator />;
}
